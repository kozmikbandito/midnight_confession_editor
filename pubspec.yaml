name: midnight_confession_editor
description: "a case editor for the game"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0' # Flutter'ın modern versiyonları için bu aralık daha iyidir.

dependencies:
  flutter:
    sdk: flutter

  # Bağımlılıklar burada, 'dependencies' başlığının altında olmalı.
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0


flutter:

  uses-material-design: true